export class CreateTokenCardUseCase {
  constructor(
    //private readonly cardRepository: CardRepository,
    //private readonly tokenRepository: TokenRepository,
    //private readonly tokenService: TokenService,
  ) {}
  /*
    async execute(card: Card): Promise<Token> {
      //const card = await this.cardRepository.getCardById(cardId);
      //const token = this.tokenService.createToken(card);
      //await this.tokenRepository.saveToken(token);
      //return token;
    }
    */
}